<!DOCTYPE html>
<html>
<head>
	<% include html_header.ejs %>
</head>
<body>
	<% include body_header.ejs %>
	
	<div class="jumbotron">
		<h1><%= title %></h1>
		<p class="lead">これは、非公式投票システム。<p/>簡単に投票するための「場」を作るためのシステムです。</p>
		<p><a class="btn btn-lg btn-success" href="/newform" role="button">新しい投票場を作成する</a></p>
	</div>

	<div class="container">
		<h2>最近登録された投票場</h2>
		<hr>
		<!-- loopする -->
		<% for(var i=0; i<events.length; i++) {%>
		<div class="row">
			<div class="col-xs-12 col-sm-4 col-md-2" style="margin:10px">
			    <% if(! events[i].status || events[i].status == "open"){ %>
				  <a href="vote/<%= events[i]._id %>"><%= events[i].eventName %></a>
				<% } else { %>
				  <%= events[i].eventName %>
				<% } %>
			</div>
			<div class="col-xs-12 col-sm-6 col-md-6" style="margin:10px">
				<%= events[i].eventDetail %>
			</div>
			<div class="col-xs-6 col-sm-2 col-md-2" style="margin:10px"> 
				<a href="result/<%= events[i]._id %>">[ 管理・集計 ]</a>
			</div>
		</div>
		<hr>
		<% } %>
		<!-- //loopするここまで -->

	</div>

	<% include body_footer.ejs %>

</body>
</html>